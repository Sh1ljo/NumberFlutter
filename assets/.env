# Copy to assets/.env and fill in.
# The app loads assets/.env at startup via flutter_dotenv.
# Firebase itself is configured by lib/firebase_options.dart (run `flutterfire configure`).

# Required for native Google Sign-In on Android/iOS: the Web client ID from
# Firebase → Authentication → Sign-in method → Google → Web SDK configuration.
GOOGLE_WEB_CLIENT_ID=
